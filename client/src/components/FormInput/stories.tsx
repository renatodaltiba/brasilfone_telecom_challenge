import { Story, Meta } from '@storybook/react'
import { FormInput } from '.'

export default {
  title: 'FormInput',
  component: FormInput
} as Meta

export const Default: Story = () => <FormInput />
